# 2026-09-19 可读性、悬浮入口与工厂介绍

用户明确授权三项修改：加深偏细的文字，参考 KINETOP 增加 WhatsApp / Email / 返回顶部入口，为 About Us 和 Manufacturing 增加工厂介绍。其他页面结构、产品规格、原始图片和询盘投递状态不变。

## 本轮实现

- 继续使用本地 IBM Plex Sans。正文采用 500 字重，通用标题采用 600，导航由 400 调为 500；保留已有标题字号和明确覆盖的主视觉标题样式。
- 次要正文灰色由 `#5F6973` 加深为 `#4E5B68`；浅色来源说明改用同一文字灰色。技术表头使用 600；页脚底部文字统一为 11px。
- 全站复用 `FloatingActions`：WhatsApp、Email、返回顶部。使用原生 `wa.me` / `mailto:` / `#site-top` 链接，不加聊天插件、追踪脚本或第三方依赖。用户随后要求到顶自动隐藏：仅增加轻量的滚动位置状态，初始位于顶部时不显示返回顶部按钮，向下滚动后显示，到顶后再次隐藏；WhatsApp 和 Email 保留。
- 桌面右下角竖排，手机横排，按钮最小 46px。保留键盘焦点样式与 reduced-motion 行为；手机页底预留空间，表单聚焦、手机导航或模态内容打开时隐藏悬浮控件。
- About Us 在既有公司介绍区域展示 HINGETRA 工厂定位、产品范围、制造能力和需求沟通方式，不另建重复大版块。
- Manufacturing 在既有能力概览前补两段工厂说明；能力区仍非严格工序顺序，不新增设备型号或数字承诺。

## 占位联系方式

用户回复：先用占位号码和邮箱，之后再修改。因此**仅新悬浮入口**使用演示配置，现有 Contact 页经批准的资料不在本次替换范围内。

统一文件：`src/data/quick-contact.ts`。

| 配置 | 当前值 | 状态 |
| --- | --- | --- |
| WhatsApp | `+1 202 555 0143` | 保留虚构号码，不是实际可联系的账户 |
| Email | `sales@hingetra.example` | 保留示例域名，不是实际邮箱 |
| isPlaceholder | `true` | 全站悬浮入口显示 Demo contacts，提示不是有效联系渠道 |

不使用随意编造、可能属于第三方的真实号码。NANPA 将 555-0100 至 555-0199 保留为虚构非工作号码；WhatsApp 链接采用官方国际号码格式。[NANPA](https://nanpa.com/numbering/555-line-numbers)、[WhatsApp 官方说明](https://faq.whatsapp.com/5913398998672934)

正式替换时填写真实号码和邮箱，再将 `isPlaceholder` 设为 false。应同时核对 Contact 页及其 `CONTACT_EMAIL` 等配置，保证所有入口一致；验证 WhatsApp 账号、邮箱收件和真实 RFQ 后才能上线。`verify:seo` 会拒绝启用收录但仍含演示联系入口的导出。

邮件按钮只打开访客自己的邮件客户端，不是网站后台代发；不会替代尚未连接的 RFQ 投递系统。占位状态下不应向该邮箱发送真实询盘。

## 工厂文案依据

- 品牌：用户批准的 HINGETRA / Hingetra Industrial Hinges，不推定新法律主体。
- 目录第 3 页：自有设备、车床加工、自动冲压、批量装配、标准化包装。
- 目录第 18 页：自有工厂、按客户要求开发生产、生产中使用检查工具。
- 产品结构、系列与应用：原有目录验证的数据层，第 4–15 页；包装按第 16–17 页的系列范围。
- OEM、加工和经销商询盘中的结构、尺寸、数量、图纸是需求收集指导，不是虚构的客户案例或订单成果。

保留所有工厂概念图与加工示意图的 AI / Illustrative scene 标识。没有增加工厂面积、人数、产能、创立年份、证书有效性、MOQ、载荷、材质牌号、容差、交期或出口数量。

## 用户提供正式联系渠道 — 2026-09-19 后续更新

- 用户提供邮箱 `cindy@hingetra.com`，随后补充 WhatsApp `+86 15584143652` 和二维码图片，明确要求增加独立的二维码按钮。以上值替代前文记录的演示联系方式，`isPlaceholder` 改为 `false`。
- Contact 页的邮箱默认值与悬浮入口共享同一配置，不再把新邮箱描述为目录中的联系方式。原目录联系人和语音电话保持不变；用户只确认新号码用于 WhatsApp。
- 直接聊天链接采用 `https://wa.me/8615584143652`，邮件入口采用 `mailto:cindy@hingetra.com`；只打开访客的聊天/邮件应用，不自动发送消息。
- 二维码来自用户附件 `codex-clipboard-70faddff-a590-4b88-8d0a-1df95c75b43d.png`，原样复制到 `public/contact/whatsapp-qr.png`，不重新生成、不裁剪、不经有损图片优化。
- 使用现有圆形悬浮按钮样式和浏览器原生 `popover="auto"`。点击二维码按钮打开，关闭按钮、点弹层外部和 Esc 都可关闭；移动端提供直接打开 WhatsApp 的备选链接。交互依据 [MDN Popover 文档](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes/popover)。不引入第三方聊天或二维码依赖。
- 保留到顶隐藏返回顶部按钮、既有页面布局和全站 noindex。没有配置 SMTP、邮件 API 或 RFQ 接收端，仍显示 `Request checked, not sent.`。
- 用户提供了联系方式及邮箱解析生效截图，但真实收发和 WhatsApp 目标仍需要实际确认；本轮不会主动发送任何测试消息，也不做截图或扩展视觉审核。
- 此次更新通过 79 项现有测试与包含 TypeScript 的 Cloudflare 静态生产构建。28 个 HTML 的本地链接和资源检查通过；25 个内容页均包含正式邮箱、WhatsApp 和二维码入口，不再包含旧邮箱或演示号码。二维码导出后的 SHA-256 与用户原图一致，132 条顶层文件锁定记录一致。全站仍禁止收录，RFQ 投递边界未改。

## 参考站值得借鉴的内容

[KINETOP 首页](https://kinetoptape.com/)、[About](https://kinetoptape.com/about)、[Factory](https://kinetoptape.com/factory) 的表达方式值得借鉴：联系入口容易找到；公司介绍解释具体产品与工作；采购清单和 FAQ 帮买家准备询盘。本轮应用前两项，既有 Resources、表格和 RFQ 清单继续承担第三项。

之后可考虑审核后的产品资料下载和真实加工/检测视频，需要真实、可公开的素材后再做。不照搬对方的 MOQ、样品周期、产量、认证或市场覆盖说法，也不把对方写在页面上的数字当作已独立核实的事实。

这次不改主视觉布局、不增加 AI 场景、不增加营销弹窗，不连接实际邮件服务，网站仍为 noindex 预览。按用户此前偏好，不截图或扩展四宽度视觉审核，仅保留发布所需的自动构建与完整性检查。

## 发布检查

79 项现有自动检查通过，TypeScript 与静态生产构建通过；28 个导出 HTML 的本地链接及资源检查通过。到顶隐藏改动后，SEO 检查覆盖 25 个内容页面、6 篇文章和 904 个初始 HTML 页内链接（返回顶部链接滚动后才显示）。147 条文件锁定记录的 SHA-256 均一致。未向占位 WhatsApp 或邮箱发送任何信息。
